//Evento para llamar al carrusel sólo hasta que cargue el DOM completo

/*window.addEventListener('DOMContentLoaded', function()) { 
  
  //Inicializar el carrusel (Bootstrap)

  let carouselElement = document.querySelector('#carouselHome');
  let carousel = new bootstrap.Carousel(carouselElement, {
    interval: 4000,
    ride: 'carousel' //Para iniciarse automáticamente
  });

  //Botones (manipulación manual)
  let buttonPrevious = document.querySelector('#carousel-control-prev');
  let buttonNext = document.querySelector('#carousel-control-next');

  buttonPrevious.addEventListener.addEventListener('click', function() {
    carousel.prev();
  });

  buttonNext.addEventListener.addEventListener('click', function() {
    carousel.prev();
  });


}; */